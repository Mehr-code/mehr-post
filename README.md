# 🤖 ربات تلگرام پیام ناشناس

این پروژه یک **ربات تلگرام پیام ناشناس** است که به کاربران اجازه می‌دهد بدون مشخص شدن هویت‌شان به ادمین (OWNER) پیام ارسال کنند و ادمین بتواند با **Reply** کردن، مستقیماً به همان کاربر جواب بدهد 👀✨

این ربات مناسب ساخت بات‌های اعتراف، سوالات ناشناس، یا ارتباط یک‌طرفه امن است.

---

## 🧠 ایده کلی پروژه

* کاربر پیام می‌فرستد → پیام به صورت ناشناس برای ادمین ارسال می‌شود
* ادمین روی پیام **reply** می‌کند → پاسخ به همان کاربر برمی‌گردد
* هیچ اطلاعات هویتی از کاربر به ادمین نمایش داده نمی‌شود 🔒

---

## ⚙️ تکنولوژی‌ها

* Python 🐍
* python-telegram-bot (async)
* SQLite (ذخیره تاریخچه پیام‌ها)
* aiohttp (HTTP server + keep-alive)
* dotenv (مدیریت متغیرهای محیطی)

---

## 📂 ساختار ساده پروژه

* `main.py` → منطق اصلی ربات
* `messages.db` → دیتابیس SQLite برای ذخیره پیام‌ها
* `.env` → متغیرهای محیطی (توکن و آی‌دی ادمین)

---

## 🚀 نحوه اجرا

### 1️⃣ نصب وابستگی‌ها

```bash
pip install python-telegram-bot aiohttp python-dotenv
```

### 2️⃣ ساخت فایل `.env`

```env
BOT_TOKEN=your_telegram_bot_token
OWNER_ID=your_telegram_user_id
PORT=10000
```

### 3️⃣ اجرای ربات

```bash
python main.py
```

---

## ✨ قابلیت‌ها

* دریافت پیام ناشناس از کاربران
* ارسال پیام به ادمین بدون افشای هویت
* پاسخ‌دهی ادمین با Reply
* ذخیره تاریخچه پیام‌ها در SQLite
* HTTP dummy server برای سازگاری با Render
* keep-alive ping برای جلوگیری از sleep شدن سرور 💤🚫

---

## 🛠️ نکات فنی مهم

* ارتباط پیام‌ها با کاربران از طریق `anonymous_map` مدیریت می‌شود
* برای جلوگیری از Conflict در Render، خطای `telegram.error.Conflict` مدیریت شده
* ربات کاملاً async است و برای deployment سبک طراحی شده

---

## 🔮 ایده‌های توسعه آینده (TODO)

* [ ] رمزنگاری پیام‌ها
* [ ] پنل مدیریتی برای ادمین
* [ ] حذف تاریخچه قدیمی
* [ ] پشتیبانی از چند ادمین
* [ ] ارسال فایل و ویس ناشناس

---

## 👤 سازنده

ساخته شده با ☕ و کمی دیوانگی برنامه‌نویسی 😄

اگر ایده‌ای برای بهتر شدنش داری، خوشحال می‌شم بدونم 🚀
